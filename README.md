# A keyboard layout switcher for KDE Plasma


Supports config files for customization 

## How to use
1- Build the program
2- In KDE settings -> shortcut: add New application short cut
3- for customization you can use a .TOML or a .YAML file in path ~/.config/layout-pie/config.toml
> NOTE: an example config file is already in the files
